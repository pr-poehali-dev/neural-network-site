import { useState, useRef, useEffect } from 'react';
import { Button } from '@/components/ui/button';
import { Textarea } from '@/components/ui/textarea';
import Icon from '@/components/ui/icon';

interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: Date;
}

const Index = () => {
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const messagesEndRef = useRef<HTMLDivElement>(null);
  const textareaRef = useRef<HTMLTextAreaElement>(null);

  const scrollToBottom = () => {
    messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
  };

  useEffect(() => {
    scrollToBottom();
  }, [messages]);

  useEffect(() => {
    textareaRef.current?.focus();
  }, []);

  const searchOnline = async (query: string): Promise<string> => {
    try {
      const response = await fetch('https://functions.poehali.dev/d2db74d3-6334-43fb-a7e9-4fe5ab0c274b', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({ query }),
      });

      if (!response.ok) {
        return '';
      }

      const data = await response.json();
      return data.answer || '';
    } catch (error) {
      return '';
    }
  };

  const getAIResponse = (userInput: string): string => {
    const input = userInput.toLowerCase();
    
    if (input.includes('linkin park') || input.includes('линкин парк')) {
      if (input.includes('кто') || input.includes('что за') || input.includes('расскажи')) {
        return 'Linkin Park — американская рок-группа из Калифорнии, основанная в 1996 году. Стала одной из самых успешных групп 2000-х, продав более 100 миллионов альбомов по всему миру.';
      }
      if (input.includes('состав') || input.includes('участники') || input.includes('кто входит')) {
        return 'Основной состав: Майк Шинода (вокал, рэп), Честер Беннингтон (вокал, 1976-2017), Брэд Делсон (гитара), Дэйв Фаррелл (бас), Джо Хан (диджей), Роб Бурдон (ударные). После трагической смерти Честера в 2017 году группа взяла паузу.';
      }
      if (input.includes('альбом') || input.includes('песн') || input.includes('хит')) {
        return 'Главные альбомы: Hybrid Theory (2000), Meteora (2003), Minutes to Midnight (2007), A Thousand Suns (2010). Хиты: "In The End", "Numb", "Crawling", "What I\'ve Done", "Breaking The Habit".';
      }
      if (input.includes('жанр') || input.includes('стиль')) {
        return 'Linkin Park сочетали ню-метал, альтернативный рок, электронную музыку и хип-хоп. Их звучание эволюционировало от агрессивного ню-метала к более мелодичному альтернативному року.';
      }
      return 'Linkin Park — легендарная рок-группа, оставившая огромный след в музыке 2000-х. Что именно вас интересует: история, участники, альбомы или стиль?';
    }
    
    if (input.includes('честер') || input.includes('chester') || input.includes('беннингтон')) {
      return 'Честер Беннингтон (1976-2017) — вокалист Linkin Park с невероятно эмоциональным голосом. Его мощный скриминг и чувственные баллады стали визитной карточкой группы. Трагически ушёл из жизни в 2017 году.';
    }
    
    if (input.includes('майк') || input.includes('шинода') || input.includes('shinoda')) {
      return 'Майк Шинода — сооснователь Linkin Park, MC, продюсер и художник. Отвечал за рэп-партии, продюсирование и визуальное оформление группы. После смерти Честера продолжил сольную карьеру и возглавил проекты группы.';
    }

    if (input.includes('космос') || input.includes('вселенн') || input.includes('галактик')) {
      if (input.includes('чёрн') && input.includes('дыр')) {
        return 'Чёрные дыры — это области пространства-времени с такой мощной гравитацией, что даже свет не может их покинуть. Образуются после коллапса массивных звёзд. Ближайшая к Земле — Gaia BH1, на расстоянии около 1600 световых лет.';
      }
      if (input.includes('марс')) {
        return 'Марс — четвёртая планета от Солнца, известная как "Красная планета" из-за оксида железа на поверхности. На Марсе есть самая высокая гора в Солнечной системе — вулкан Олимп высотой 21 км. Сейчас там работают марсоходы NASA.';
      }
      return 'Наша Вселенная возникла около 13.8 миллиардов лет назад в результате Большого взрыва. Млечный Путь содержит 200-400 миллиардов звёзд, а во Вселенной примерно 2 триллиона галактик!';
    }

    if (input.includes('skybattle') || input.includes('скайбатл')) {
      if (input.includes('что это') || input.includes('описание') || input.includes('режим')) {
        return 'SkyBattle Arena — популярный PvP-режим на серверах Minecraft. Игроки сражаются на летающих островах в небе, используя оружие, зелья и тактику. Цель — сбросить противников с платформ или победить в бою. IP для подключения: skybattle.ru или centrix.su';
      }
      if (input.includes('правила') || input.includes('как играть') || input.includes('геймплей')) {
        return 'В SkyBattle игроки спавнятся на островах с базовым снаряжением. Нужно собирать ресурсы с генераторов, улучшать броню и оружие, атаковать противников. Побеждает последний выживший игрок или команда. Есть режимы Solo (1v1v1v1) и Team (2v2v2v2).';
      }
      if (input.includes('сервер') || input.includes('ip') || input.includes('адрес') || input.includes('подключ')) {
        return 'Сервер SkyBattle работает в сети CentrixPVP. IP для подключения: centrix.su или skybattle.ru (порт стандартный 25565). Версии Minecraft: 1.8-1.20+. Онлайн достигает нескольких тысяч игроков одновременно.';
      }
      if (input.includes('донат') || input.includes('приват') || input.includes('вип')) {
        return 'На SkyBattle есть донат-привилегии: VIP, PREMIUM, ELITE с различными бонусами (киты, косметика, увеличенные награды). Можно играть полностью бесплатно, донат даёт лишь визуальные и небольшие игровые преимущества.';
      }
      return 'SkyBattle Arena — PvP-режим Minecraft на летающих островах. Популярен в России и СНГ. Что тебя интересует: правила игры, как подключиться, или особенности сервера?';
    }

    if (input.includes('гугл') || input.includes('google')) {
      if (input.includes('основ') || input.includes('созда') || input.includes('начало') || input.includes('истори')) {
        return 'Google основан в 1998 году студентами Стэнфорда Ларри Пейджем и Сергеем Брином. Они разработали алгоритм PageRank в гараже Сьюзан Войжитски. Название — игра слов от "googol" (число из единицы и ста нулей), символизирующее огромный объём информации.';
      }
      if (input.includes('сервис') || input.includes('продукт') || input.includes('что умеет')) {
        return 'Продукты Google: поиск (обрабатывает 8.5 млрд запросов/день), Gmail (1.8 млрд пользователей), YouTube (куплен в 2006 за $1.65 млрд), Google Maps, Android (86% рынка смартфонов), Chrome (65% рынка браузеров), Google Drive.';
      }
      if (input.includes('доход') || input.includes('деньги') || input.includes('прибыль') || input.includes('капитализ')) {
        return 'Alphabet (материнская компания Google с 2015 года) зарабатывает более $280 млрд в год. 80% дохода — от рекламы. Капитализация — около $1.7 триллиона. Первый офис арендовали за $1700/месяц, сейчас компания стоит триллионы!';
      }
      if (input.includes('пейдж') || input.includes('page') || input.includes('ларри')) {
        return 'Ларри Пейдж — соучредитель Google, создатель алгоритма PageRank. Был CEO до 2001 и с 2011 по 2019. Его состояние — около $100 млрд. Известен инновационными проектами: беспилотные авто (Waymo), интернет из стратосферы (Project Loon).';
      }
      if (input.includes('брин') || input.includes('сергей')) {
        return 'Сергей Брин — соучредитель Google, эмигрировал из СССР в 1979 году. Отвечал за технологии поиска и инфраструктуру. Его состояние — около $95 млрд. Активно инвестирует в биотехнологии и исследование болезни Паркинсона.';
      }
      if (input.includes('android') || input.includes('андроид')) {
        return 'Android куплен Google в 2005 году за $50 млн. Первый Android-смартфон вышел в 2008. Сейчас на Android работает 86% всех смартфонов в мире — более 3 млрд устройств. Это самая популярная ОС в истории.';
      }
      if (input.includes('youtube') || input.includes('ютуб')) {
        return 'YouTube куплен Google в 2006 за $1.65 млрд — одна из лучших сделок в истории. Сейчас YouTube стоит более $180 млрд. Каждую минуту загружается 500 часов видео, ежедневно смотрят 1 млрд часов контента.';
      }
      return 'Google — крупнейшая поисковая система мира (92% рынка). Что тебя интересует: история создания, продукты, основатели или финансы?';
    }

    if (input.includes('искусственный интеллект') || input.includes('ии') || input.includes('нейросет')) {
      return 'Искусственный интеллект — это системы, способные выполнять задачи, требующие человеческого интеллекта: распознавание образов, принятие решений, понимание языка. Современные нейросети, как GPT и DALL-E, используют глубокое обучение на огромных датасетах.';
    }

    if (input.includes('динозавр') || input.includes('тиранозавр') || input.includes('юрский')) {
      return 'Динозавры господствовали на Земле 165 миллионов лет, с триасового по меловой период (252-66 млн лет назад). Вымерли после падения астероида диаметром 10 км в районе Мексики. Птицы — прямые потомки динозавров!';
    }

    if (input.includes('футбол') || input.includes('месси') || input.includes('роналду')) {
      return 'Лионель Месси и Криштиану Роналду — два величайших футболиста современности. Месси выиграл 8 Золотых мячей, Роналду — 5. Месси стал чемпионом мира 2022 с Аргентиной, завершив свою коллекцию трофеев.';
    }

    if (input.includes('программирование') || input.includes('код') || input.includes('python') || input.includes('javascript')) {
      return 'Программирование — создание инструкций для компьютеров. Python популярен для ИИ и анализа данных, JavaScript — для веб-разработки. Первый программист — Ада Лавлейс, написавшая алгоритм для машины Бэббиджа в 1843 году!';
    }

    if (input.includes('квантов') || input.includes('физик')) {
      return 'Квантовая физика изучает поведение частиц на атомном уровне. Квантовая суперпозиция позволяет частице находиться в нескольких состояниях одновременно. Квантовые компьютеры используют это для решения сверхсложных задач.';
    }

    if (input.includes('египет') || input.includes('пирамид') || input.includes('фараон')) {
      return 'Древний Египет — одна из древнейших цивилизаций (3100 до н.э. - 30 до н.э.). Великая пирамида Хеопса в Гизе была построена около 2560 года до н.э. и оставалась самым высоким сооружением 3800 лет. Использовано 2.3 млн каменных блоков!';
    }

    if (input.includes('океан') || input.includes('мариан') || input.includes('глубин')) {
      return 'Марианская впадина — самая глубокая точка океана, 11 034 метра. Давление там в 1000 раз больше атмосферного! Мировой океан покрывает 71% Земли, но исследовано только 5% его глубин — меньше, чем поверхность Луны.';
    }

    if (input.includes('обновлен') || input.includes('апдейт') || input.includes('новая версия')) {
      return 'Да, скоро выйдет сергиндоус SPT 2! 🚀';
    }

    if (input.includes('климат') || input.includes('глобальное потепление') || input.includes('парниковый')) {
      return 'Глобальное потепление вызвано увеличением парниковых газов (CO2, метан) в атмосфере. С 1880 года средняя температура Земли выросла на 1.1°C. Арктический лёд тает со скоростью 13% за десятилетие.';
    }

    if (input.includes('книг') || input.includes('литератур') || input.includes('толстой') || input.includes('достоевск')) {
      return 'Русская классика — сокровище мировой литературы. "Война и мир" Толстого охватывает историю целой эпохи, "Преступление и наказание" Достоевского исследует психологию преступника. Обе книги входят в топ-100 величайших произведений всех времён.';
    }

    if (input.includes('кино') || input.includes('фильм') || input.includes('оскар')) {
      return 'Киноиндустрия зародилась в конце XIX века. "Оскар" вручается с 1929 года. Рекордсмен по "Оскарам" — "Бен-Гур", "Титаник" и "Властелин колец: Возвращение короля" — по 11 статуэток каждый. Российский "Левиафан" был номинирован в 2015.';
    }

    if (input.includes('здоровь') || input.includes('медицин') || input.includes('витамин')) {
      return 'Здоровый образ жизни включает: сбалансированное питание, 7-8 часов сна, физическую активность (150 минут в неделю), достаточное потребление воды (1.5-2 литра в день). Витамин D особенно важен в зимний период.';
    }

    if (input.includes('биткоин') || input.includes('криптовалют') || input.includes('блокчейн')) {
      return 'Биткоин — первая криптовалюта, созданная в 2009 году Сатоши Накамото (личность неизвестна). Блокчейн — распределённая база данных, где записи невозможно изменить. Биткоин достигал цены более $69,000 в 2021 году.';
    }

    if (input.includes('собак') || input.includes('кошк') || input.includes('питомц')) {
      return 'Собаки были одомашнены около 15,000 лет назад, кошки — около 9,000 лет назад. Собаки могут различать до 250 слов, кошки спят 12-16 часов в сутки. Самая популярная порода собак — лабрадор-ретривер.';
    }

    if (input.includes('япон') || input.includes('токио') || input.includes('самурай') || input.includes('аниме')) {
      return 'Япония — островное государство с богатой культурой. Токио — крупнейшая городская агломерация мира (38 млн человек). Самураи были военным сословом с XII по XIX век. Аниме — японская анимация, популярная во всём мире.';
    }

    if (input.includes('шахмат') || input.includes('карпов') || input.includes('каспаров')) {
      return 'Шахматы — одна из древнейших игр, возникла в Индии в VI веке. Гарри Каспаров был чемпионом мира 15 лет (1985-2000). В 1997 году компьютер Deep Blue от IBM впервые обыграл чемпиона мира. Россия доминирует в шахматах на протяжении десятилетий.';
    }

    if (input.includes('автомобил') || input.includes('машин') || input.includes('тесла') || input.includes('электрокар')) {
      return 'Электромобили становятся популярнее: Tesla лидирует в сегменте премиум-электрокаров. Первый электромобиль создан в 1828 году! Современные электрокары проезжают 400-600 км на одном заряде, заряжаются за 30-60 минут.';
    }

    if (input.includes('горы') || input.includes('эверест') || input.includes('альпы') || input.includes('гималаи')) {
      return 'Эверест — высочайшая гора Земли (8849 м). Находится в Гималаях на границе Непала и Тибета. Впервые покорён Эдмундом Хиллари и Тенцингом Норгеем в 1953 году. Температура на вершине опускается до -60°C.';
    }

    if (input.includes('еда') || input.includes('кухн') || input.includes('рецепт') || input.includes('готовить')) {
      return 'Кулинария — искусство приготовления пищи. Французская кухня считается одной из лучших в мире. Итальянская паста, японские суши, грузинский хачапури, русские блины — каждая культура внесла вклад в мировую гастрономию.';
    }

    if (input.includes('интернет') || input.includes('wi-fi') || input.includes('веб') || input.includes('сайт')) {
      return 'Интернет создан в 1969 году как военный проект ARPANET. Первый веб-сайт появился в 1991 году. Сейчас интернетом пользуются более 5 миллиардов человек — 63% населения Земли. Wi-Fi работает на частотах 2.4 и 5 ГГц.';
    }

    if (input.includes('погод') || input.includes('дождь') || input.includes('снег') || input.includes('температур')) {
      return 'Погода формируется атмосферными процессами. Самая высокая температура на Земле зафиксирована в Долине Смерти — +56.7°C, самая низкая в Антарктиде — -89.2°C. Молния нагревается до 30,000°C — в 5 раз горячее поверхности Солнца!';
    }

    if (input.includes('война') || input.includes('великая отечественная') || input.includes('1945')) {
      return 'Великая Отечественная война (1941-1945) — часть Второй мировой войны. СССР потерял 27 миллионов человек. Решающие битвы: Сталинградская (1942-43), Курская дуга (1943), битва за Берлин (1945). День Победы — 9 мая 1945 года.';
    }

    if (input.includes('математик') || input.includes('формул') || input.includes('уравнение')) {
      return 'Математика — царица наук. Число Пи (π) ≈ 3.14159... бесконечно и не повторяется. Теорема Пифагора: a² + b² = c² работает только для прямоугольных треугольников. Великие математики: Евклид, Эйлер, Гаусс, Ковалевская.';
    }

    return '';
  };

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (!input.trim() || isLoading) return;

    const userMessage: Message = {
      id: Date.now().toString(),
      role: 'user',
      content: input.trim(),
      timestamp: new Date(),
    };

    setMessages((prev) => [...prev, userMessage]);
    const userQuestion = input.trim();
    setInput('');
    setIsLoading(true);

    const onlineAnswer = await searchOnline(userQuestion);
    const response = onlineAnswer || 'Извините, я не нашёл информацию по вашему вопросу. Попробуйте переформулировать запрос.';

    const aiMessage: Message = {
      id: (Date.now() + 1).toString(),
      role: 'assistant',
      content: response,
      timestamp: new Date(),
    };
    setMessages((prev) => [...prev, aiMessage]);
    setIsLoading(false);
  };

  const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSubmit(e);
    }
  };

  return (
    <div className="flex flex-col h-screen bg-background">
      <header className="border-b border-border px-6 py-4">
        <div className="max-w-3xl mx-auto flex items-center gap-3">
          <div className="w-8 h-8 rounded-lg bg-primary flex items-center justify-center">
            <Icon name="Sparkles" size={18} className="text-white" />
          </div>
          <div>
            <h1 className="text-xl font-semibold">сергиндоус SPT 1</h1>
          </div>
        </div>
      </header>

      <main className="flex-1 overflow-y-auto px-6 py-8">
        <div className="max-w-3xl mx-auto space-y-6">
          {messages.length === 0 ? (
            <div className="flex flex-col items-center justify-center h-[60vh] text-center">
              <div className="w-16 h-16 rounded-2xl bg-primary/10 flex items-center justify-center mb-6">
                <Icon name="MessageSquare" size={32} className="text-primary" />
              </div>
              <h2 className="text-2xl font-semibold mb-2">Начните диалог</h2>
              <p className="text-muted-foreground">
                Задайте любой вопрос — сергиндоус SPT 1 ответит
              </p>
            </div>
          ) : (
            messages.map((message) => (
              <div
                key={message.id}
                className={`fade-in flex gap-4 ${
                  message.role === 'user' ? 'justify-end' : 'justify-start'
                }`}
              >
                {message.role === 'assistant' && (
                  <div className="w-8 h-8 rounded-full bg-primary flex items-center justify-center flex-shrink-0 mt-1">
                    <Icon name="Bot" size={18} className="text-white" />
                  </div>
                )}
                <div
                  className={`rounded-2xl px-5 py-3 max-w-[80%] ${
                    message.role === 'user'
                      ? 'bg-primary text-white'
                      : 'bg-card border border-border'
                  }`}
                >
                  <p className="text-[15px] leading-relaxed whitespace-pre-wrap">
                    {message.content}
                  </p>
                </div>
                {message.role === 'user' && (
                  <div className="w-8 h-8 rounded-full bg-secondary flex items-center justify-center flex-shrink-0 mt-1">
                    <Icon name="User" size={18} className="text-foreground" />
                  </div>
                )}
              </div>
            ))
          )}
          {isLoading && (
            <div className="fade-in flex gap-4">
              <div className="w-8 h-8 rounded-full bg-primary flex items-center justify-center flex-shrink-0 mt-1">
                <Icon name="Bot" size={18} className="text-white" />
              </div>
              <div className="rounded-2xl px-5 py-3 bg-card border border-border">
                <div className="flex gap-1">
                  <div className="w-2 h-2 bg-muted-foreground rounded-full animate-pulse" />
                  <div className="w-2 h-2 bg-muted-foreground rounded-full animate-pulse delay-75" />
                  <div className="w-2 h-2 bg-muted-foreground rounded-full animate-pulse delay-150" />
                </div>
              </div>
            </div>
          )}
          <div ref={messagesEndRef} />
        </div>
      </main>

      <footer className="border-t border-border px-6 py-4">
        <form onSubmit={handleSubmit} className="max-w-3xl mx-auto">
          <div className="flex gap-3 items-end">
            <Button
              type="submit"
              disabled={!input.trim() || isLoading}
              className="h-[52px] px-6 bg-primary hover:bg-primary/90"
            >
              <Icon name="Send" size={18} />
            </Button>
            <Textarea
              ref={textareaRef}
              value={input}
              onChange={(e) => setInput(e.target.value)}
              onKeyDown={handleKeyDown}
              placeholder="Напишите сообщение..."
              className="min-h-[52px] max-h-[200px] resize-none bg-card border-border focus-visible:ring-primary"
              rows={1}
            />
          </div>
          <p className="text-xs text-muted-foreground mt-2 text-center">
            Нажмите Enter для отправки, Shift+Enter для новой строки
          </p>
        </form>
      </footer>
    </div>
  );
};

export default Index;